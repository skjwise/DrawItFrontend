import React, { Component } from 'react';

export class MyDrawings extends Component {
    render() {
        return (
            <div>
                my drawings
            </div>
        );
    }
}

export default MyDrawings;
